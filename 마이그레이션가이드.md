Antigravity 프로젝트 연동 및 아키텍처 마이그레이션 가이드본 가이드는 단일 파일로 구성된 프로토타입(index.html)의 핵심 기능(VR 환경 가시화, AI 자율 저작, 에이전트 기반 거래 메커니즘)을 프로덕션 레벨의 모듈화 프로젝트인 Antigravity 아키텍처로 이관하여 개발을 지속하기 위한 기술 로드맵을 제시합니다.1. 지향할 프로덕션 프로젝트 구조 (Modular Architecture)단일 파일의 모노리스 구조를 기능별로 엄격히 관심사 분리(Separation of Concerns)를 하여 아래와 같은 현대적 번들러(Vite/Webpack + TypeScript 권장) 구조로 재편합니다.antigravity-project/
├── public/
│   ├── assets/            # 3D 모델(GLTF/OBJ), 오디오 소스 등 정적 자산
│   └── favicon.ico
├── src/
│   ├── main.js            # 애플리케이션 진입점 및 전역 상태 초기화
│   ├── index.css          # LYT 모드 디자인 시스템 및 Tailwind 설정
│   │
│   ├── engine/            # 1단계: 3D 기하/커널 비주얼라이제이션 엔진
│   │   ├── ThreeRenderer.js # Scene, Camera, Light, 렌더 루프 제어
│   │   ├── EnvironmentManager.js # 도시, 실내, 시골 환경 절차적 빌더
│   │   └── InteractionRaycaster.js # Raycasting 및 마우스/터치 물리 반응
│   │
│   ├── ai/                # 2단계: AI 에이전트 및 저작 프로그램 스크립팅
│   │   ├── PromptInterpreter.js # LLM API 통신 및 프롬프트 분석
│   │   ├── ProceduralGenerator.js # 프롬프트 맞춤형 3D 기하 메쉬 생성기
│   │   └── AgentSimulator.js  # 자율 에이전트 행동 및 백그라운드 스케줄러
│   │
│   ├── blockchain/        # 3단계: 가상화폐 지갑 및 자산 소유권 거래
│   │   ├── WalletConnector.js # Web3 지갑(Metamask, Web3Auth) 연동
│   │   └── ContractService.js # ERC-721/1155 NFT 스마트 컨트랙트 통신
│   │
│   └── ui/                # UI 컴포넌트 레이어
│       ├── Layout.js      # Sliding Panes 레이아웃 관리
│       ├── Inspector.js   # 선택 사물 속성 표시기
│       └── MarketView.js  # 자율 크리에이터 마켓플레이스 패널
│
├── package.json
└── vite.config.js
2. 단계별 마이그레이션 실행 전략[1단계] 3D 그래픽스 및 물리 인터랙션 이관 (src/engine/)프로토타입에 내장되어 있던 Three.js의 전역 변수 구조를 OOP(객체지향) 또는 클래스 구조로 캡슐화합니다.ThreeRenderer 클래스 구현:window.onload에 의존하던 렌더링 루프를 클래스 인스턴스 메서드로 정의합니다.WebGLRenderer의 그림자 속성(shadowMap) 및 광원 설계를 컴포넌트화하여 전역 네온 테마 광원을 격리합니다.Raycaster 이벤트 모듈화:도어(Door), 컴퓨터(Computer), 컵(Cup) 등 상호작용 가능한 객체들의 userData 명세를 규격화하여 InteractionRaycaster.js로 분리합니다.클릭이나 터치 이벤트가 트리거되었을 때, 메인 스레드에 이벤트를 전송하는 콜백 구조로 변경합니다.[2단계] AI 저작 도구의 실제 LLM API 연동 (src/ai/)가짜 프롬프트 파서 대신, 실제 Gemini API 또는 Claude API를 탑재하여 실시간 기하 생성 파이프라인을 구축합니다.실제 연산 플로우 예시:사용자가 "가로등" 또는 "비닐하우스" 프롬프트 입력 후 AI Build 요청.PromptInterpreter.js에서 LLM API를 호출하여 구조화된 JSON 데이터를 응답받음.응답 스키마 형태:{
  "geometryType": "cylinder",
  "dimensions": { "radiusTop": 0.1, "radiusBottom": 0.12, "height": 3 },
  "emissiveColor": "#00F0FF",
  "physicsScript": "function animate(mesh, time) { mesh.rotation.y = time * 0.002; }"
}
ProceduralGenerator.js가 이 JSON 사양에 따라 Three.js 기본 도형을 조합하여 커널 기반의 3D Object3D를 인스턴스화하고 Scene에 추가합니다.physicsScript 문자열은 new Function 안전 검증(Sandboxing)을 거쳐 렌더 루프에 바인딩됩니다.[3단계] 분산 에이전트와 블록체인 거래소 연동 (src/blockchain/)단순 상태 변수(userBalance, agentBalance)로 처리하던 지갑 연동을 실질적인 블록체인 SDK로 업그레이드합니다.사용자 지갑 연동:ethers.js 또는 viem 라이브러리를 설치하여 브라우저의 MetaMask 등의 지갑 연결을 활성화합니다.0xUser...F8a1 임시 주소를 실제 사용자의 지갑 주소(window.ethereum.selectedAddress)로 교체합니다.AI 에이전트의 자율 계약 체결:에이전트의 프라이빗 키는 안전한 백엔드(Serverless, Node.js 등) 혹은 분산 보안 보관소(KMS)에 보관합니다.에이전트가 자율 창작한 3D 사물 정보와 물리 스크립트를 IPFS(또는 Arweave) 분산 저장소에 업로드하고 메타데이터 URI를 확보합니다.에이전트의 프라이빗 키 서명을 통해 스마트 컨트랙트의 mint 함수를 호출하여 가상현실 콘텐츠를 NFT 형태로 마켓플레이스에 등록하게 설계합니다.3. 핵심 디자인 시스템(LYT Mode)의 프로덕션 빌드 이식가독성과 정보 밀도를 극대화하는 LYT Mode(Linking Your Thinking) 테마의 미감을 완성도 있게 유지하기 위한 CSS 마이그레이션 규칙입니다.Tailwind CSS 색상 및 타이포그래피 설정 (tailwind.config.js 적용 시):module.exports = {
  theme: {
    extend: {
      colors: {
        'lyt-bg-main': '#060608',
        'lyt-bg-pane': '#0c0c12',
        'lyt-bg-card': '#14141f',
        'lyt-accent-yellow': '#FFD700',
        'lyt-accent-purple': '#B57EDC',
        'lyt-accent-cyan': '#00F0FF',
      },
      fontFamily: {
        sans: ['"Avenir Next"', 'Inter', 'sans-serif'],
        mono: ['"DM Mono"', 'monospace'],
      },
      lineHeight: {
        'lyt-tight': '1.35', // 시선 분산을 방지하는 정보 가독성 보장
      }
    }
  }
}
수평적 사고 유도용 글로우 효과 유지:CSS의 text-shadow 및 box-shadow를 활용하여 링크 및 하이라이트(mark) 요소를 화면 위로 부각시킵니다.슬라이딩 패인(Sliding Panes) 레이아웃의 액티브 탭 테두리 효과를 컴포넌트 포커스 시 동일하게 부여합니다.